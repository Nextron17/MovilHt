<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    android:fitsSystemWindows="true"
    tools:context=".view.InvernaderoActivity">

    <LinearLayout
        android:id="@+id/main_content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:elevation="0dp">
            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/colorBackground"
                app:titleTextColor="@color/textColorPrimary"/>
        </com.google.android.material.appbar.AppBarLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="24dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Invernaderos"
                android:textSize="34sp"
                android:textStyle="bold"
                android:textColor="@color/textColorPrimary"/>

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvInvernaderos"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginTop="16dp"/>
        </LinearLayout>
    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@drawable/drawer_background"
        app:elevation="8dp"
        app:headerLayout="@layout/nav_header"
        app:itemIconPadding="24dp"
        app:itemHorizontalPadding="24dp"
        app:itemVerticalPadding="16dp"
        app:itemShapeFillColor="@android:color/transparent"
        app:itemShapeAppearanceOverlay="@style/ShapeAppearance.App.SmallComponent"
        app:itemTextAppearance="@style/NavDrawerTextStyle"
        app:itemTextColor="@color/nav_item_color_selector"
        app:itemIconTint="@color/nav_item_color_selector"
        app:itemBackground="@drawable/nav_item_background"
        app:menu="@menu/nav_menu" />
</androidx.drawerlayout.widget.DrawerLayout>